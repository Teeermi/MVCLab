services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: ${SECRET_KEY:-tajny_klucz_aplikacji}
    volumes:
      - ./data.json:/app/data.json
      - ./users.json:/app/users.json
